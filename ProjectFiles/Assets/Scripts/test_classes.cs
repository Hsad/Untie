using UnityEngine;
using System.Collections;

public class test_classes : MonoBehaviour {
	private json_parse.Assasinate k = new json_parse.Assasinate(); 
	// Use this for initialization
	void Start () {
		k.success();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
